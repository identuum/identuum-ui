# UI drift census — identuum-ui vs the RELEASED identuum-idp-oss v0.3.0

Measured 2026-08-07/08 (THE-UI-DRIFT-CENSUS). **Measurement only — nothing was fixed.**

## The backend (order A)

The released artifact, exactly as a customer gets it: `deployment/docker-compose.yml`
(the flipped pin) on a **fresh disposable DB**, image

```
ghcr.io/identuum/identuum-idp-oss@sha256:44114fb25e77a1efece7999ccb8bbe869ebd47005a580f8d7d2a21814558ce4e  (:v0.3.0)
```

UI under test: this checkout, served by the suite's own webServer (`next dev`).

## The suite runs (order B)

Population: **29 git-tracked OSS-scope specs / 170 tests** (33 tracked minus 4 `ce-*`;
the `.next/standalone/e2e` copies are build output and excluded).

**Run pair 1 — the documented default (`npx playwright test`), twice:** every browser
test failed with `ERR_CONNECTION_REFUSED at http://localhost:7114`. The gitignored
`.env.playwright.idp-oss.local` (consumed by the config; never read by this census)
sets a base URL on **:7114** while the webServer default starts on **:7104** — the
suite's own documented default run is broken by owner-local env residue. 6 passed /
64 failed / 100 skipped, identically twice. Every failure one cause; zero UI signal.

**Run pair 2 — aligned (`IDENTUUM_E2E_PORT=7114`, the config's own shell-precedence
mechanism; no files changed):**

| spec | pass | fail | skip | failure mode (first error) |
|---|---|---|---|---|
| account-settings | 7 | 1 | 11 | beforeAll login timeout (creds not on fresh backend) |
| claim | 2 | 0 | 0 | |
| dashboard | 1 | 0 | 0 | |
| health-and-redirects | 6 | 0 | 0 | |
| login | 8 | 1 | 0 | full-flow: Password step never rendered (email step got no valid user) |
| org-admin-api-resources | 0 | 0 | 9 | self-skip (dynamic fixture off) |
| org-admin-applications | 0 | 0 | 8 | self-skip |
| org-admin-service-accounts | 0 | 0 | 8 | self-skip |
| org-admin-settings | 0 | 0 | 12 | self-skip |
| org-admin-smoke | 0 | 0 | 10 | self-skip |
| org-admin-user-detail | 0 | 0 | 4 | self-skip |
| org-admin | 3 | 0 | 0 | |
| oss-change-password | 0 | 0 | 1 | self-skip (creds) |
| oss-contract | 5 | 0 | 0 | **all green — see STALE-PIN below** |
| oss-setup-wizard | 0 | 0 | 1 | self-skip (opt-in) |
| oss-site-admin-smoke | 0 | 0 | 1 | self-skip (script-only) |
| passkey-flow | 0 | 1 | 4 | beforeAll login timeout |
| platform-status | 11 | 0 | 5 | |
| setup-wizard | 0 | 0 | 1 | self-skip (opt-in, mutates DB) |
| site-admin-admin-recovery | 0 | 1 | 3 | beforeAll login timeout |
| site-admin-audit | 0 | 1 | 2 | beforeAll login timeout |
| site-admin-mfa-enrollment | 0 | 0 | 1 | self-skip |
| site-admin-observability | 0 | 7 | 0 | beforeEach login timeout |
| site-admin-organizations-crud | 0 | 0 | 1 | self-skip |
| site-admin-organizations | 0 | 1 | 19 | beforeAll login timeout |
| site-admin-settings | 0 | 1 | 0 | CONNECTION_REFUSED (dev server died late in run) |
| upgrade-backup-live | 0 | 0 | 4 | self-skip (script-only) |
| upgrade-backup | 0 | 4 | 0 | CONNECTION_REFUSED (dev server died) |
| verify-email | 0 | 4 | 0 | CONNECTION_REFUSED (dev server died) |
| **TOTAL** | **43** | **22** | **105** | **= 170 ✓** (38.6 min) |

Confirm run: see the flake column note at the end of this file (appended after the
second aligned run completed).

**Failure decomposition — no failure is a UI-behaviour bug; every one is harness/state
coupling:** (a) authenticated specs log in with env-file credentials that only exist on
the owner's long-lived seeded stack — against a FRESH released backend they time out in
`beforeAll` (~180 s each; those timeouts are what stretched the run to 38 minutes);
(b) the tail cluster (site-admin-settings, upgrade-backup, verify-email) failed on
`ERR_CONNECTION_REFUSED` — the `next dev` webServer died partway through the run;
(c) the org-admin family self-skips because dynamic-fixture mode is off — and could
not be turned on: its global-setup preflight probes `/app/identuum` and an `/e2e-auth`
bind mount inside the IDP container, which is the PRE-SPLIT MONOLITH contract; the
released distroless appliance has `/app/identuum-idp` and no such mount.

## STALE-PIN: the UI's own contract spec asserts the scaffold era

`oss-contract.spec.ts` passed 5/5 against v0.3.0 — and two of those passes are
vacuous-by-shape (V2): the header still reads *"No auth, no /authorize, no /token"* —
verbatim the "old scaffold lie" that identuum-idp-oss's own
`TestOSSContract_NoScaffoldFraming` bans from its main.go — and the negative pins
(`GET /authorize MUST NOT be 200/302`, `POST /token MUST NOT be 200`) go green
against a backend that SERVES both endpoints, because a parameterless probe gets a
4xx. The spec certifies the absence of the product's core feature and passes while
the feature ships.

## API-surface diff (order C)

Predicates: SERVED = one `(method, path)` under `paths:` in
`identuum-idp-oss/openapi.yaml` (parsed, count **130**). CALLED = a `fetch()` in
tracked `src/**/*.ts(x)` (tests excluded) whose URL contains `idpBaseUrl`/`IDP_BASE`;
`${…}` segments → `{param}`; query dropped; `/license*` (CE surface) excluded
(count **71**). Match = same method + same path with placeholders wildcarded.

**MATCHED 52 · SERVED-NO-UI 78 · CALLED-NOT-SERVED 19.
Sums: 52+78 = 130 = SERVED ✓ · 52+19 = 71 = CALLED ✓**

SERVED-NO-UI by feature area (78 total):

| n | feature area |
|---|---|
| 12 | auth/session (incl. lockout, refresh, browser-login) |
| 7 | OAuth protocol (authorize/token/introspect/revoke/PAR-shape) |
| 6 | RBAC roles |
| 5 | signing keys · 5 service-accounts global verbs (enable/disable/update) · 5 self-service (me/*) |
| 4 each | upstream IdP config · scope templates · WebAuthn management · OIDC protocol |
| 3 each | setup wizard (verify/status beyond UI use) · login MFA · back-channel-logout delivery admin |
| 2 each | users · discovery · organizations · WebAuthn login · component/validate |
| 1 each | health details · /system/info · service-account org verb |

CALLED-NOT-SERVED (19 — UI calls with no OSS operation behind them; several are
CE-conditional surfaces the UI probes, which is itself the finding — the OSS build
carries dead call sites):
anomaly events/stats · audit + audit/event-types · system/audit/chain/verify ·
system/sessions · health/details · jobs/{id} · organizations/{id}/identity-providerS ·
organizations/{id}/webhooks · organizations/{id}/invitations ·
org service-accounts PATCH/DELETE (org-scoped verbs vs OSS's global verbs) ·
auth/change-password (POST; OSS serves a different shape) · auth/users/setup ·
users/{id}/mfa/reset · users/{id}/setup/resend · logout (POST /api/v1/logout vs
OSS's /api/v1/auth/logout) · organizations/{param} POST (extraction shows no serving
op; carrier in idp-admin-client).

## MANUAL-TEST.md mapping (order D)

Predicate: COVERED = a spec exercises the same behaviour through the UI;
AFFORDANCE-ONLY = the page/affordance renders but the action is deliberately not
performed; NONE = no spec touches it. **64 checks = 23 COVERED + 9 AFFORDANCE-ONLY +
32 NONE ✓**

| area | COVERED | AFFORDANCE-ONLY | NONE |
|---|---|---|---|
| authn (9) | 5 (authn.1,3,5,7,9) | 0 | 4 (wrong-password, org_user first login, bad TOTP, lockout) |
| authz (11) | 2 (authz.1,9) | 1 (authz.11 assign-admin) | 8 (every refusal matrix row) |
| organizations (6) | 3 (create, rename, soft-delete) | 3 (deactivate/reactivate/restore confirm pages) | 0 |
| users (10) | 4 (list, claim-invalid, resend, verify-invalid) | 2 (assign-admin, bulk-invite drawer) | 4 (update, ban, banned-login, reset-request) |
| clients (9) | 6 | 3 (create/update/delete affordances) | 0 |
| oidc (18) | 3 (discovery, jwks, browser-session) | 0 | 15 (the ENTIRE OAuth/OIDC protocol matrix — and oidc.6/oidc.7 are pinned BACKWARDS by the stale contract spec) |
| ratelimit (1) | 0 | 0 | 1 |

## Gap classes (one wiki ledger row each)

1. **HARNESS-PRESPLIT** — the authenticated-fixture harness (global-setup preflight,
   e2e/README) speaks the pre-split monolith's container contract.
2. **SEEDED-COUPLING** — no green path exists against a fresh released backend;
   authenticated specs presuppose the owner's seeded stack + local env residue
   (including the :7114/:7104 port split that breaks the documented default run).
3. **STALE-CONTRACT-PINS** — oss-contract certifies the scaffold era and passes
   vacuously against the released product.
4. **API-SURFACE** — 78 served operations with no UI; 19 UI calls with no serving
   operation.
5. **MANUAL-COVERAGE** — 32 of 64 manual checks have no covering spec; the OIDC
   protocol matrix is 15/18 uncovered.

## Confirm run (flake marking)

Second aligned run: **52 pass / 13 fail / 105 skip = 170 ✓** (37.9 min).
26/29 specs byte-identical across the two runs. The three that differ —
site-admin-settings (1), upgrade-backup (4), verify-email (4), all
`ERR_CONNECTION_REFUSED` in run 1 of the pair, all PASS in run 2 — are the
dev-server-death tail: **FLAKE, harness class (b), not UI behaviour.** The 13
stable failures are all credential-coupling class (a): one login full-flow plus
twelve `beforeAll`/`beforeEach` login timeouts across the site-admin family and
passkey-flow. The confirm run also surfaced (in the webServer log, not as a test
failure) a React hydration mismatch warning — recorded as an observation only.

**Stable verdict against the released v0.3.0 backend: 43–52 tests can pass, 13
fail solely because the suite cannot authenticate against a fresh backend, 105
skip. Zero failures indict the UI's behaviour; every one indicts the harness's
coupling to a pre-seeded, pre-split-era stack.**

---

# AFTER — THE-RELEASED-CONTRACT (2026-08-08)

The harness was rebuilt to build fixtures against the RELEASED appliance over
its HTTP API (orders A/B). Same instrument, same 29 tracked OSS specs, dynamic
mode on, `--workers=1`, against a FRESH v0.3.0 appliance stood up from nothing.

## Suite totals — before → after

| | pass | fail | skip | total | wall |
|---|---|---|---|---|---|
| BEFORE (aligned, env-file, standing stack) | 43 | 22 | 105 | 170 | 38.6 min |
| AFTER (API fixtures, fresh released appliance) | **99** | **21** | **51** | 171¹ | **6.9 min** |

¹ one additional test executes now that a describe block's conditional test runs
instead of being skipped whole. `host.docker.internal` errors: 18 → **0**.

## The 68-test conversion (order C)

The 13 stable credential-coupling failures + the 55 org-admin skips = 68 dead
tests. Their disposition now:

- **site-admin family (was 13 login-timeout failures):** site-admin-observability
  0/7-timeout → **7/7 PASS**; site-admin-audit → 3 PASS; site-admin-settings →
  1 PASS; account-settings 7→**19 PASS**. Residual real failures:
  site-admin-admin-recovery (2), site-admin-organizations (1) — now genuine
  signal, classified below.
- **org-admin family (was 55 all-skip):** now RUNS end to end — org-admin-smoke
  5P/4F, org-admin-settings 6P/6F, org-admin-service-accounts 3P/4F,
  org-admin-api-resources 3P/1F, org-admin-applications 2P, org-admin-user-detail
  2P/1F, org-admin 3P. The majority PASS; the failures are genuine UI signal.

Every one of the 68 converted: to PASS, or to a CLASSIFIED failure (below). None
remains a harness timeout or a blanket skip.

## The 21 residual failures, classified

1. **GENUINE UI DRIFT (feeds UI-DRIFT-API-SURFACE)** — pages that render/behave
   differently against the OSS backend than the specs expect: org-admin-smoke
   `/org-admin/users` (heading/tabs, and a filter-tab timeout), `/org-admin/settings`
   card count, `/org-admin/audit` shape; org-admin-settings + api-resources +
   service-accounts SAFE-STATE render pins. These are the UI calling org surfaces
   the OSS build serves differently or not at all (the census's CALLED-NOT-SERVED
   set — org domains/webhooks/identity-providers/invitations). Real drift, now
   visible because the pages finally load under a real session.
2. **FIXTURE-DEPTH (new residual, UI-DRIFT-FIXTURE-DEPTH)** — the `[dynamic mode
   only]` sub-tests that need SEEDED sample entities (public/confidential OAuth
   clients, an API resource, a service account) which the core envelope does not
   build. They run (dynamic mode is on) and fail on the absent entity rather than
   self-skipping. The envelope carries org + admin + user; the optional seeded
   blocks are a follow-up.
3. **HARNESS IPv6 residue (1)** — site-admin-organizations "Details link…" does an
   in-spec `apiRequestContext.post` to the UI at localhost:7104 that resolves to
   IPv6 `::1` and ECONNREFUSEs (the same localhost/IPv6 hazard the webServer probe
   documents). Env residue, not a UI defect.
4. **WebAuthn (1)** — passkey-flow "register a passkey" times out on the CDP
   virtual authenticator against the released appliance; needs its own setup.
5. **login full-flow (1)** — one assertion in the end-to-end login spec; the other
   8 login tests pass.

## What this proves

A fresh clone + Docker now reaches an authenticated green run from NOTHING: the
published v0.3.0 image on a throwaway DB, all credentials minted via the released
API. The census's top two classes (HARNESS-PRESPLIT, SEEDED-COUPLING) are closed;
STALE-CONTRACT-PINS is closed (oss-contract flipped to positive shipped-surface
pins). The failures that remain are the product's REAL UI-vs-backend drift plus a
fixture-depth follow-up — signal the old harness could never reach because it
never authenticated against the shipped artifact at all.

## Confirm run (flake marking)

The two AFTER runs are BYTE-IDENTICAL — 99 pass / 21 fail / 51 skip, all 29
specs matching. **Zero flakes: every one of the 21 failures is stable,
deterministic signal**, not environment noise. (Contrast the BEFORE pair, whose
only differences were the dev-server-death tail.)

---

# AFTER-2 — THE-ALL-GREEN-SUITE (2026-08-08)

Backend unchanged: the PUBLISHED v0.3.0 image (digest `44114fb2…`). Every claim
below was measured against it.

## The auth-transport discovery (root cause of most of the 21)

Released OSS establishes the request principal ONLY from `Authorization: Bearer`
(`mw.BearerPrincipal` never reads the `access_token` cookie; only
`/api/v1/validate` is cookie-aware). The UI forwarded cookies on every IdP call,
so admin shells rendered (validate passed) while every data panel 401'd.
Proven: the same login-issued org_admin token → 200 as Bearer, 401 as cookie on
`/clients`, `/users`, `/organizations/:id`. Owner ruling (gated on that proof):
UI-BUILDS-IT, server-side only — the BFF modules and the `/api/idp` browser
proxy lift the caller's own httpOnly `access_token` into a Bearer header on the
server→IdP hop; the token never reaches browser JS.

## The 21 residual failures — disposition (sums to 21)

| cluster (from the AFTER classification) | n | disposition |
|---|---|---|
| GENUINE UI DRIFT (smoke users/tabs/settings/audit; settings + api-resources + service-accounts render pins) | 12 | **UI-BUILDS-IT — LANDED.** Bearer transport + released-shape readers (org top-level, `organization_domains`, bare SA array, `/audit/events`, plural→singular identity-provider, add-domain `txt_record_*`) + real-surface locators (tab counts, role=status banner, 4th Save, innerText authority scan). All 12 now PASS. |
| FIXTURE-DEPTH `[dynamic mode only]` sub-tests | 6 | **UI-BUILDS-IT (seeding) — LANDED**: public + confidential OAuth clients seeded via the released API by the org_admin. Residual asserts inside 5 of them hit two OSS response gaps (below) — owner-ruled HONEST REDS, not skips. |
| IPv6-localhost residue (site-admin-organizations request context) | 1 | **UI-BUILDS-IT — LANDED** (127.0.0.1; plus the psql-time-warp expired-org fixture is best-effort so its absence self-skips the two conditional tests instead of sinking 19 others). PASSES. |
| WebAuthn passkey register (CDP virtual authenticator) | 1 | **UI-BUILDS-IT — LANDED.** Proxy bearer + appliance WebAuthn origin follows the suite's one authoritative baseURL + released login-finish envelope accepted + served logout path + per-IP rate-limit env knobs. passkey-flow now passes **5/5** (register, persist, sign-in, delete) — a real pass, not a skip. |
| login full-flow assertion | 1 | **UI-BUILDS-IT — LANDED** (cross-tier `/healthz`→`/health` status probe; login-page hydration mount-gate). PASSES. |

No row dispositioned DEAD-CALL-REMOVED or CE-ONLY-GATED at test level (the CE
class lives in the ce-* specs outside the census's 29 tracked OSS specs);
OSS-GAINS-IT appears at the assertion level inside the fixture-depth residue.

## Suite totals — the slice's own before → after (same instrument, --workers=1)

| | pass | fail | skip | did-not-run |
|---|---|---|---|---|
| session start (three-credential envelope, pre-fix) | 104 | 30 | 25 | 22 |
| after the tail (run 1) | **141** | **15** | 25 | **0** |

## The 15 remaining reds — every one a named backend gap (sums to 15)

| gap | n | tests |
|---|---|---|
| **A — `safeClient` omits `is_public`** (domain stores it; response drops it; `token_endpoint_auth_method` unreliable) — v0.3.1 DECIDED | 4 | applications 112/183/377/614 |
| **B — resource-subject audit events absent** (`client.secret_rotated` + `service_account.updated` unsubject-tagged; `service_account_linked_oauth_client` never emitted) — v0.3.1 DECIDED (client rotate; SA family same mechanism) | 3 | applications 698, service-accounts 632, 232 |
| **C — org self-update is site_admin-only** (`PUT /organizations/:id` under `RequireSiteAdmin`; org_admin cannot save profile/MFA/invite policy of its OWN org — at odds with the admin model) — v0.3.x candidate | 1 | settings 300 |
| **D — primary `organization_domains` row not materialized at org creation** (Domains card can never show the Primary+Verified row) — v0.3.x candidate | 2 | settings 588 (tail assert; its remove-flow half passes), 688 |
| **E — SA name uniqueness not enforced** (rename-to-duplicate returns 200; the UI's 409 inline-error path is unreachable) — v0.3.x candidate | 1 | service-accounts 769 |
| **CE-ONLY-GATED** (CE license/M1 stack absent on the OSS appliance; ce-* untouched per the bar) | 4 | ce-customer-smoke ×3, ce-fresh-m1 ×1 |

Owner ruling: v0.3.1 = EXACTLY gaps A+B (additive, separate authorized OSS
slice; suite re-points to :v0.3.1). C/D/E are reported candidates — left RED,
never skipped, never papered over in the UI.

## API-surface dispositions (UI-DRIFT-API-SURFACE close)

**CALLED-NOT-SERVED — 19, itemized. UI-BUILDS-IT 7 (5 landed, 2 remaining) +
CE-ONLY-GATED 8 + OSS-GAINS-IT 3 + DEAD-CALL-REMOVED 1 = 19 ✓**

| call | disposition |
|---|---|
| GET /api/v1/audit | UI-BUILDS-IT — LANDED (→ `/audit/events`, limit/offset + start/end) |
| organizations/{id}/identity-providers (plural) | UI-BUILDS-IT — LANDED (CE plural list first, OSS singular fallback; 404-none → empty list) |
| org-scoped SA PATCH | UI-BUILDS-IT — LANDED (→ `PUT /service-accounts/:id`; org scope is server-derived from actor+row, cross-org reads 404) |
| org-scoped SA DELETE | UI-BUILDS-IT — LANDED (→ `DELETE /service-accounts/:id`) |
| POST /api/v1/logout | UI-BUILDS-IT — LANDED (→ served `/api/v1/auth/logout`) |
| auth/change-password (shape) | UI-BUILDS-IT — REMAINING (align to the served shape) |
| users/{id}/mfa/reset | UI-BUILDS-IT — REMAINING (served handler exists; path shape differs) |
| anomaly events · anomaly stats · audit/event-types · system/audit/chain/verify · system/sessions · health/details · jobs/{id} · organizations/{id}/webhooks | CE-ONLY-GATED (8 — CE-conditional probes; every consuming section degrades gracefully on OSS) |
| organizations/{id}/invitations · auth/users/setup · users/{id}/setup/resend | OSS-GAINS-IT (3 — BLOCKED-OWNER: the invite / manual-setup-link flow family has no released OSS surface; UI affordances render, performing flows need an owner tier decision) |
| organizations/{param} POST carrier | DEAD-CALL-REMOVED (extraction artifact; no runtime call site) |

**SERVED-NO-UI — 78, by the census's own areas. Sums: 31+13+13+10+11 = 78 ✓**

| class | n | areas |
|---|---|---|
| MATCHED-VIA-PROXY (extraction undercount: consumed through the `/api/idp` browser proxy / IDP_PATHS constants the census's `idpBaseUrl`-only predicate missed) | 31 | auth/session 12 · self-service me/* 5 · WebAuthn management 4 · setup wizard 3 · login MFA 3 · WebAuthn login 2 · component/validate 2 |
| MATCHED-EXTRACTION-MISS (consumed via idp-admin-client / api-status paths the extractor mismatched) | 10 | signing keys 5 · scope templates 4 (read-only section) · health details 1 |
| NO-UI-BY-DESIGN (protocol/machine surfaces; browsers and OAuth clients consume them, not the admin UI — explicit disposition class added by this slice) | 13 | OAuth protocol 7 · OIDC protocol 4 · discovery 2 |
| UI-BUILDS-IT — LANDED | 5 | service-account global verbs 5 (the UI now calls exactly these) |
| UI-BUILDS-IT — REMAINING | 19 | RBAC roles 6 · upstream IdP config 4 (read side landed cross-tier) · backchannel-logout admin 3 · users 2 · organizations 2 · /system/info 1 · SA org verb (with-client) 1 |

## Confirm run

Run 1: 141 pass / 15 fail / 25 skip / 0 did-not-run (3.6 min). Run 2 recorded
alongside; the 15-red set is the stable, fully-dispositioned floor of the
released v0.3.0 backend — the '0 FAIL' bar is gated on v0.3.1 (gaps A+B) by
owner decision, with C/D/E as reported candidates.
